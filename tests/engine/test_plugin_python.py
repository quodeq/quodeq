"""Plugin tests for the Python evaluator plugin.

The tests for the Python plugin are implemented in ``test_plugin_java.py``
via ``pytest.mark.parametrize``, which covers both the ``java`` and ``python``
plugins in a single parametrized suite.  Run ``pytest tests/engine/test_plugin_java.py``
to execute both sets of plugin tests.
"""
