// Dialog action-button visual styles shared by utils/chooseDialog.js and
// utils/confirmDialog.js's DOM-built modal actions. No backend mirror: a
// purely presentational choice this pair of files renders as class-name
// suffixes (qd-confirm-btn--<variant>).
export const DIALOG_VARIANT = Object.freeze({ DEFAULT: 'default', PRIMARY: 'primary', DANGER: 'danger' });
