// The product name is identity, not translatable copy: it reads the same in
// every locale and must not drift per language. Kept out of en.json so no
// translator is ever handed it as a string to render.
export const BRAND_NAME = 'quodeq';
