/**
 * Label placement shared by the non-galaxy map views (RiskMatrixView's
 * bubbles and ZoomablePackView's circles). Both sit a name just above a
 * round shape, and the two gaps drifting apart would make the same map read
 * differently from one view to the next.
 */

/** Gap between a shape's top edge and its label baseline, in pixels. */
export const LABEL_GAP_PX = 4;
