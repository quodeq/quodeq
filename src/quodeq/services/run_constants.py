"""Run-selection constants shared by services and the api routes that call them."""
from __future__ import annotations

LATEST_RUN = "latest"  # ?run= sentinel meaning "the newest run", in services and every api route default
