"""Re-export for backward compatibility -- moved to quodeq.analysis.plugins.detector."""
from quodeq.analysis.plugins.detector import (
    count_source_files,
    detect_plugin,
    list_source_files,
)
